<UserControl x:Class="QuanLyHocSinh.View.Controls.QuyDinh.QuyDinhSuaUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuanLyHocSinh.View.Controls.QuyDinh"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#F4F6F8" >
        <materialDesign:Card  Padding="24"  HorizontalAlignment="Center" VerticalAlignment="Center" Width="500">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Sửa Quy Định"
                           FontSize="22"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,20"
                           Foreground="#2C3E50"/>

                <Grid Grid.Row="1" Margin="0,0,0,10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="160"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Tuổi Tối Thiểu:" Grid.Row="0" Grid.Column="0"
                               VerticalAlignment="Center" Margin="0,8"/>
                    <TextBox Text="{Binding TuoiToiThieu, UpdateSourceTrigger=PropertyChanged}"
                             Grid.Row="0" Grid.Column="1"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             materialDesign:HintAssist.Hint="VD: 6"/>

                    <TextBlock Text="Tuổi Tối Đa:" Grid.Row="1" Grid.Column="0"
                               VerticalAlignment="Center" Margin="0,8"/>
                    <TextBox Text="{Binding TuoiToiDa, UpdateSourceTrigger=PropertyChanged}"
                             Grid.Row="1" Grid.Column="1"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             materialDesign:HintAssist.Hint="VD: 15"/>

                    <TextBlock Text="Sĩ Số Lớp:" Grid.Row="2" Grid.Column="0"
                               VerticalAlignment="Center" Margin="0,8"/>
                    <TextBox Text="{Binding SiSoLop, UpdateSourceTrigger=PropertyChanged}"
                             Grid.Row="2" Grid.Column="1"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             materialDesign:HintAssist.Hint="VD: 30"/>

                    <TextBlock Text="Số Lượng Môn Học:" Grid.Row="3" Grid.Column="0"
                               VerticalAlignment="Center" Margin="0,8"/>
                    <TextBox Text="{Binding SoLuongMonHoc, UpdateSourceTrigger=PropertyChanged}"
                             Grid.Row="3" Grid.Column="1"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             materialDesign:HintAssist.Hint="VD: 12"/>

                    <TextBlock Text="Điểm Đạt:" Grid.Row="4" Grid.Column="0"
                               VerticalAlignment="Center" Margin="0,8"/>
                    <TextBox Text="{Binding DiemDat, UpdateSourceTrigger=PropertyChanged}"
                             Grid.Row="4" Grid.Column="1"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             materialDesign:HintAssist.Hint="VD: 5.0"/>

                    <TextBlock Text="Quy Định Khác:" Grid.Row="5" Grid.Column="0"
                               VerticalAlignment="Center" Margin="0,8"/>
                    <TextBox Text="{Binding QuyDinhKhac, UpdateSourceTrigger=PropertyChanged}"
                             Grid.Row="5" Grid.Column="1"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             materialDesign:HintAssist.Hint="Ghi chú thêm nếu cần"/>
                </Grid>

                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button Content="Cập nhật"
                            Command="{Binding UpdateCommand}"
                            Margin="0,0,10,0"
                            Width="100"
                            Background="#2196F3" Foreground="White"/>
                    <Button Content="Hủy"
                            Command="{Binding CancelCommand}"
                            Width="100"
                            Background="#E53935"
                            Foreground="White"/>

                </StackPanel>
            </Grid>
        </materialDesign:Card>
    </Grid>
</UserControl>
